= link_to "Create New Link", new_link_path if current_user
%br
- @links.each do |link|
  .link_title= link_to link.title, link.url if (link.title.present? && link.url.present?)
  .link_url= link_to link.url, link.url if (link.url.present? && !link.banner.file? )
  .link_banner= link_to image_tag( link.banner.url), link.url if (link.banner.file?  && link.url.present?)
  %br
  %hr