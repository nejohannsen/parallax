= link_to "Create New Photo", new_photo_path if current_user
%br
- @photos.each do |photo|
  .photo_banner= link_to image_tag( photo.image.url(:thumb)), photo_path(photo)
  .photo_title= link_to photo.title, photo_path(photo)
  %br